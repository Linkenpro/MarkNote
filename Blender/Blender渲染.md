#### 渲染实用技巧

##### 灯光排除

1. 选中灯光
2. 物体
3. 着色方式
4. 新建灯光链接
5. 在选中灯光的状态下，拖动模型物体添加至灯光链接排除组中

##### 材质库搭建

**1.建立材质源文件**

专门创建一个`.blend `文件作为你的材质仓库

文件里创建常用的材质

给这些材质起好名字，并给它们一个有代表性的预览图

**2.标记为资产**

在 Outliner（大纲视图） 或 Shader Editor（着色器编辑器） 窗口中，找到做好的材质

右键点击 材质名称，选择 Mark as Asset（标记为资产）

标记成功后，材质图标旁边会出现一个小书本形状的图标

**3.配置资产路径**

打开 **Edit（编辑）** -> **Preferences（偏好设置）**，快捷键`Ctrl`+`,`

在左侧选择 **File Paths（文件路径）**。

找到 **Asset Libraries（资产库）** 栏目，点击 **+** 号。

选择你存放 `MyMaterialLibrary.blend` 文件的那个**文件夹**，并给它起个名字（例如“我的常用材质”）。

点击 **Save Preferences** 保存设置。

#### 着色器编辑器节点

##### 输入

###### 环境光遮蔽（AO）

*内部参数：*

- 采样值：默认值16

  > 越高，噪点越少，计算更慢
  >
  > 一般32或64

- 向内：选项框

  > 检测外部遮挡，
  >
  > 有时有用，产品渲染很少用

- 仅内部：选项框

*主要输入：*

- 颜色：

- 距离：最重要的参数，默认值1.00

  > Distance = 0.01>>>按键缝隙，接口缝隙，装配缝隙；
  >
  > Distance = 1.0>>>整个物体之间都会互相影响，对于产品设计0.001~0.05m

- 法向：

*主要输出：*

- 颜色：

  > AO 节点设置了颜色,AO计算；输出被AO影响后的颜色
  >
  > 实际工作里很少直接用

- AO：灰度值，白色 = 暴露区域；黑色 = 遮挡区域

  > AO 	>>>	ColorRamp	>>>	Mix Color

###### 属性

从模型或几何数据中读取某个属性（Attribute），并在材质里使用

*主要输出*

- 颜色：输出颜色数据

  > 顶点颜色

- 矢量：输出向量数据

  > 法线方向、位置、速度

- 系数：输出单通道数值

  > 遮罩、AO值、权重

- Alpha

  > 

*内部参数*

- 视图层：

  1. 几何数据
  2. 物体
  3. 实例
  4. 视图层

- 名称：输入名称

  > 最重要的输入框

**使用教程**

- 读取顶点颜色
- 读取几何节点生成的属性
- 

###### 倒角

模拟边缘圆角的法线效果，而不是真的修改模型

*内部参数*

- 采样：

  > 采样越高倒角越精细

*主要输入*

- 半径：倒角半径
- 法向：

*主要输出*

- 法向

**使用教程**

- 倒角	>>>		法向	>>>		原理化BSDF

###### 摄像机数据

用于获取当前渲染摄像机相关的信息，常用于根据摄像机位置或视角控制材质效果

*主要输出*

- 视图矢量：摄像机位置 - 当前着色点位置

  > 归一化后的方向向量
  >
  > 常见用途：菲涅尔效果、根据观察角度改变颜色、模拟边缘发光、NPR卡通渲染

- Z向视图深度：当前点到摄像机的深度

  > 距离雾效
  > 远近颜色变化
  > 景深辅助遮罩

- 视图距离：当前像素到摄像机的真实空间距离

  > 

###### 颜色

常用节点，颜色拾取盘

*主要输出*

- 颜色：颜色值输出

  > 

###### 颜色属性

*内部参数*

- 颜色属性：输入框

*主要输出*

- 颜色：输出颜色信息，RGBA
- Alpha：

###### 曲线信息

###### 菲涅尔

###### 几何数据

###### 层权重

###### 光程

###### 物体信息

###### 粒子信息

###### 点信息

###### 光线投射

###### 切向（正切）

###### 纹理坐标

###### UV贴图

###### 值

*主要输出*

- 值：数值输入框

  > 手动输入数值

**常用用法**

- 封装节点组

###### 体积信息

*主要输出*

- 颜色
- 密度
- 火焰
- 色温

###### 线框

*内部参数*

- 像素尺寸：选项框

*主要输入*

- 尺寸：默认值0.01；

  > 

*主要输出*

- 系数

  > 

##### 输出

###### AOV输出

> 减少反重复渲染的时间成本
>
> 产品后期神器

- 名称：
- 颜色：遮罩图层的颜色
- 值：一般不调整

**使用教程**

1. 视图层>>>通道>>>着色器 AOV

   > 点击+号，添加通道，名称自己填，例子：1

2. 选中物件，添加>>>输出>>>AOV输出

   > 名称：里面填入与着色器 AOV相同的名字：1
   >
   > 颜色：选择自己喜欢的颜色，纯色，高饱和度

3. 渲染>>>GPU,F12渲染完成以后，可以获得颜色通道图，可以一层一层的存，也可以合并保存

   > 方便PS，Nuke，AE、达芬奇等，做产品图片的后期

**常用用法**

- 用AO增强接缝
- 做积灰遮罩
- 做磨损材质

**总结**

AO 节点更多是用来做材质细节遮罩和后期控制，而不是代替真实光照

###### 材质输出

- 目标：
  1. 全部：所有渲染器共用，最常见
  2. Eevee：只给 EEVEE 使用
  3. Cycles：只给 Cycles 使用

- 表（曲）面：日常材质输出，**最终渲染看到的材质效果**

- 体积：控制物体内部空间

  > 常见用途：体积雾，牛奶，香水瓶，乳液，磨砂玻璃

- 置换：真正改变几何表面，表面真的凸起与凹陷，非视觉假象

  > Bump为视觉凹凸；Displacement：网格在变动
  >
  > 织物、编织结构、浮雕Logo

- 厚（宽）度

###### 灯光输出

*主要输入：*

- 目标：
  1. 全部：所有渲染器共用，最常见
  2. Eevee：只给 EEVEE 使用
  3. Cycles：只给 Cycles 使用

- 表（曲）面：灯光输出

**使用教程**

- 点光，面光，聚光灯，日光
- 在着色编辑器状态栏中，勾选使用节点后出现